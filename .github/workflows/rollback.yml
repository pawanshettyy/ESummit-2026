name: CD Disabled - Rollback

on:
  workflow_dispatch:

jobs:
  cd-disabled-rollback:
    runs-on: ubuntu-latest

    steps:
      - name: CD disabled
        run: |
          echo "Rollback workflow disabled because continuous deployment configuration was removed."
          echo "If you need rollback capabilities, restore the previous workflow." 