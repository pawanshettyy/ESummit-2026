name: CD Disabled - Production

on:
  workflow_dispatch:

jobs:
  cd-disabled-production:
    runs-on: ubuntu-latest

    steps:
      - name: CD disabled
        run: |
          echo "Continuous deployment to production has been disabled in the repository."
          echo "If you need to re-enable CD, restore the original workflow file."