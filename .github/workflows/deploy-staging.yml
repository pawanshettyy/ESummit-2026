name: CD Disabled - Staging

on:
  workflow_dispatch:

jobs:
  cd-disabled-staging:
    runs-on: ubuntu-latest

    steps:
      - name: CD disabled
        run: |
          echo "Continuous deployment to staging has been disabled in the repository."
          echo "If you need to re-enable CD, restore the original workflow file."