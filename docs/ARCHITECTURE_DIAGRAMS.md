# E-Summit 2026 - Complete System Architecture Diagram

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              CLIENT LAYER                                    │
│                                                                              │
│  ┌────────────────┐  ┌────────────────┐  ┌────────────────┐                │
│  │   Web Browser  │  │  Mobile Browser │  │  Admin Panel   │                │
│  │   (React App)  │  │   (Responsive)  │  │  (React Admin) │                │
│  └────────┬───────┘  └────────┬────────┘  └────────┬───────┘                │
│           │                   │                     │                        │
│           └───────────────────┼─────────────────────┘                        │
│                               │                                              │
└───────────────────────────────┼──────────────────────────────────────────────┘
                                │
                                │ HTTPS (SSL/TLS)
                                │
┌───────────────────────────────▼──────────────────────────────────────────────┐
│                          CDN / LOAD BALANCER                                 │
│                        (Cloudflare / Nginx)                                  │
│                                                                              │
│  ┌─────────────────────────────────────────────────────────────────┐        │
│  │  - SSL Termination                                              │        │
│  │  - DDoS Protection                                              │        │
│  │  - Static Asset Caching                                         │        │
│  │  - Request Distribution                                         │        │
│  └─────────────────────────────────────────────────────────────────┘        │
└───────────────────────────────┬──────────────────────────────────────────────┘
                                │
                    ┌───────────┼───────────┐
                    │           │           │
┌───────────────────▼──┐  ┌─────▼──────┐  ┌▼────────────────┐
│   API Server 1       │  │ API Server │  │  API Server 3   │
│   (Node.js/Express)  │  │     2      │  │ (Node.js/Express)│
│                      │  │            │  │                 │
│  ┌────────────────┐  │  │            │  │                 │
│  │ Controllers    │  │  │            │  │                 │
│  │ - Auth         │  │  │            │  │                 │
│  │ - Pass Booking │  │  │            │  │                 │
│  │ - Events       │  │  │            │  │                 │
│  │ - Admin        │  │  │            │  │                 │
│  └────────┬───────┘  │  │            │  │                 │
│           │          │  │            │  │                 │
│  ┌────────▼───────┐  │  │            │  │                 │
│  │ Services       │  │  │            │  │                 │
│  │ - Payment      │  │  │            │  │                 │
│  │ - QR Code      │  │  │            │  │                 │
│  │ - Email        │  │  │            │  │                 │
│  │ - Analytics    │  │  │            │  │                 │
│  └────────┬───────┘  │  │            │  │                 │
│           │          │  │            │  │                 │
│  ┌────────▼───────┐  │  │            │  │                 │
│  │ Middleware     │  │  │            │  │                 │
│  │ - Auth JWT     │  │  │            │  │                 │
│  │ - Validation   │  │  │            │  │                 │
│  │ - Rate Limit   │  │  │            │  │                 │
│  │ - Error Handler│  │  │            │  │                 │
│  └────────────────┘  │  │            │  │                 │
└──────────┬────────────┘  └─────┬──────┘  └─────┬───────────┘
           │                     │               │
           └─────────────────────┼───────────────┘
                                 │
        ┌────────────────────────┼─────────────────────────┐
        │                        │                         │
        │                        │                         │
┌───────▼─────────┐   ┌──────────▼──────────┐   ┌─────────▼──────────┐
│   PostgreSQL    │   │      Redis          │   │  AWS S3 / Cloud    │
│   (Primary DB)  │   │   (Cache/Session)   │   │   File Storage     │
│                 │   │                     │   │                    │
│  ┌───────────┐  │   │  ┌──────────────┐  │   │  ┌──────────────┐  │
│  │  Tables:  │  │   │  │ - Sessions   │  │   │  │ - QR Codes   │  │
│  │           │  │   │  │ - Cache      │  │   │  │ - Certificates│ │
│  │ - users   │  │   │  │ - Rate Limit │  │   │  │ - Documents  │  │
│  │ - passes  │  │   │  │ - Temp Data  │  │   │  │ - Images     │  │
│  │ - events  │  │   │  └──────────────┘  │   │  └──────────────┘  │
│  │ - trans.. │  │   │                     │   │                    │
│  │ - admins  │  │   │                     │   │                    │
│  │ - ...     │  │   │                     │   │                    │
│  └───────────┘  │   │                     │   │                    │
│                 │   │                     │   │                    │
│  Read Replicas  │   │  Redis Cluster      │   │  CDN Distribution  │
│  (if needed)    │   │  (if needed)        │   │                    │
└─────────────────┘   └─────────────────────┘   └────────────────────┘


┌─────────────────────────────────────────────────────────────────────────────┐
│                        EXTERNAL SERVICES                                     │
│                                                                              │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐   │
│  │  Razorpay    │  │  SendGrid/   │  │   Twilio     │  │    Sentry    │   │
│  │  (Payments)  │  │   AWS SES    │  │    (SMS)     │  │   (Errors)   │   │
│  │              │  │   (Email)    │  │  (Optional)  │  │              │   │
│  │ - Orders     │  │              │  │              │  │ - Error      │   │
│  │ - Payments   │  │ - Pass Conf. │  │ - Event      │  │   Tracking   │   │
│  │ - Refunds    │  │ - Reminders  │  │   Reminders  │  │ - Performance│   │
│  │ - Webhooks   │  │ - Receipts   │  │ - OTP        │  │   Monitoring │   │
│  └──────────────┘  └──────────────┘  └──────────────┘  └──────────────┘   │
└─────────────────────────────────────────────────────────────────────────────┘


┌─────────────────────────────────────────────────────────────────────────────┐
│                    MONITORING & LOGGING                                      │
│                                                                              │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐                      │
│  │   Winston    │  │  Prometheus  │  │   Grafana    │                      │
│  │  (Logging)   │  │  (Metrics)   │  │ (Dashboard)  │                      │
│  └──────────────┘  └──────────────┘  └──────────────┘                      │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 🔄 Data Flow Examples

### Example 1: User Registration & Login

```
┌────────┐
│ Client │
└───┬────┘
    │
    │ 1. POST /auth/register
    │    { email, password, name }
    ▼
┌────────────────┐
│  API Server    │
│                │
│  Controller    │──▶ 2. Validate input (Zod)
│      │         │
│      ▼         │
│  Auth Service  │──▶ 3. Hash password (bcrypt)
│      │         │
│      ▼         │
│   Prisma ORM   │──▶ 4. INSERT INTO users
│      │         │
│      ▼         │
│  PostgreSQL    │
└────┬───────────┘
     │
     │ 5. Generate verification token
     ▼
┌──────────────┐
│ Email Service│──▶ 6. Send verification email
│  (SendGrid)  │
└──────────────┘
     │
     ▼
┌────────┐
│ Client │──▶ 7. Success response
└────────┘


LOGIN FLOW:
┌────────┐
│ Client │
└───┬────┘
    │ 1. POST /auth/login
    │    { email, password }
    ▼
┌────────────────┐
│  API Server    │
│                │
│  Controller    │──▶ 2. Find user by email
│      │         │
│      ▼         │
│  Auth Service  │──▶ 3. Compare password (bcrypt)
│      │         │
│      ▼         │
│  JWT Utility   │──▶ 4. Generate tokens
│                │     - Access Token (15min)
│                │     - Refresh Token (7d)
└────┬───────────┘
     │
     │ 5. Return tokens + user data
     ▼
┌────────┐
│ Client │──▶ Store in memory/cookie
└────────┘
```

---

### Example 2: Pass Booking Flow

```
┌────────┐
│ Client │
└───┬────┘
    │ 1. User selects "Platinum Pass"
    │    Adds meals + workshop
    │
    │ 2. POST /passes/create-order
    │    Authorization: Bearer <token>
    │    { passType, addons }
    ▼
┌────────────────┐
│  API Server    │
│                │
│  Auth MW       │──▶ 3. Verify JWT
│      │         │
│      ▼         │
│  Pass Ctrl     │──▶ 4. Calculate total price
│      │         │     (799 + 300 + 700 = 1799)
│      ▼         │
│ Payment Svc    │──▶ 5. Create Razorpay order
└────┬───────────┘
     │
     │ 6. Razorpay API call
     ▼
┌────────────┐
│  Razorpay  │──▶ 7. Return order_id
└──────┬─────┘
       │
       │ 8. Save transaction (pending)
       ▼
┌────────────────┐
│  PostgreSQL    │
│  transactions  │
└────┬───────────┘
     │
     │ 9. Return order details to client
     ▼
┌────────┐
│ Client │──▶ 10. Open Razorpay Checkout
└───┬────┘
    │
    │ 11. User completes payment
    │
    │ 12. Razorpay callback
    │     { payment_id, order_id, signature }
    │
    │ 13. POST /passes/verify-payment
    ▼
┌────────────────┐
│  API Server    │
│                │
│ Payment Svc    │──▶ 14. Verify signature
│      │         │
│      ▼         │
│  Pass Service  │──▶ 15. Create pass record
│      │         │     - Generate pass_id
│      ▼         │     - Set status = Active
│   QR Service   │──▶ 16. Generate QR code
│      │         │
│      ▼         │
│   S3 Upload    │──▶ 17. Upload QR to S3
│      │         │
│      ▼         │
│ Email Service  │──▶ 18. Send confirmation email
│      │         │     - Digital pass PDF
│      │         │     - QR code
│      ▼         │     - Event details
│  PostgreSQL    │──▶ 19. Update transaction (completed)
└────┬───────────┘
     │
     │ 20. Return pass details
     ▼
┌────────┐
│ Client │──▶ Show success page
└────────┘     Download pass
               Add to calendar
```

---

### Example 3: Event Check-in Flow

```
ATTENDEE ARRIVES AT VENUE:
┌────────────┐
│  Attendee  │──▶ Shows QR code on phone
└──────┬─────┘
       │
       ▼
┌────────────┐
│  Scanner   │──▶ Admin scans QR
│  Operator  │    (Scanner Operator role)
└──────┬─────┘
       │
       │ 1. POST /admin/check-in
       │    Authorization: Bearer <admin_token>
       │    { passId: "ESUMMIT-2026-ABC123" }
       ▼
┌────────────────┐
│  API Server    │
│                │
│  Admin MW      │──▶ 2. Verify admin JWT
│      │         │     Check role permissions
│      ▼         │
│  Admin Ctrl    │──▶ 3. Validate pass ID
│      │         │
│      ▼         │
│   Prisma       │──▶ 4. SELECT FROM passes
└────┬───────────┘     WHERE pass_id = 'ESUMMIT...'
     │
     ▼
┌────────────────┐
│  PostgreSQL    │
│                │
│  passes table  │──▶ 5. Verify:
│                │     - Status = Active
│                │     - Not already checked in today
└────┬───────────┘
     │
     │ 6. Valid pass ✓
     ▼
┌────────────────┐
│  API Server    │
│                │
│   Prisma       │──▶ 7. INSERT INTO check_ins
│                │     - pass_id
│                │     - user_id
│                │     - check_in_time
│                │     - scanned_by (admin_id)
└────┬───────────┘
     │
     │ 8. Log audit trail
     ▼
┌────────────────┐
│  audit_logs    │──▶ 9. Record admin action
└────┬───────────┘
     │
     │ 10. Return success
     ▼
┌────────────┐
│  Scanner   │──▶ Show ✓ Check-in successful
│  Operator  │    Display attendee name
└────────────┘    Play success sound
```

---

### Example 4: Admin Analytics Dashboard

```
┌────────────┐
│ Admin User │
└──────┬─────┘
       │ 1. Opens admin panel
       │    GET /admin/dashboard/stats
       │    Authorization: Bearer <admin_token>
       ▼
┌────────────────┐
│  API Server    │
│                │
│  Admin MW      │──▶ 2. Verify JWT
│      │         │     Check role (Event Manager)
│      ▼         │
│  Admin Ctrl    │──▶ 3. Check permissions
│      │         │     analytics: true ✓
│      ▼         │
│ Analytics Svc  │──▶ 4. Check Redis cache
└────┬───────────┘
     │
     ▼
┌──────────┐
│  Redis   │──▶ Cache miss
└──────┬───┘
       │
       │ 5. Query database
       ▼
┌────────────────────────────────────────┐
│  PostgreSQL - Multiple Queries         │
│                                        │
│  Q1: SELECT COUNT(*) FROM passes       │
│      WHERE status = 'Active'           │
│                                        │
│  Q2: SELECT SUM(amount) FROM trans..   │
│      WHERE status = 'completed'        │
│                                        │
│  Q3: SELECT COUNT(*) FROM check_ins    │
│      WHERE DATE(check_in_time) = TODAY │
│                                        │
│  Q4: SELECT pass_type, COUNT(*)        │
│      FROM passes GROUP BY pass_type    │
└────┬───────────────────────────────────┘
     │
     │ 6. Aggregate results
     ▼
┌────────────────┐
│ Analytics Svc  │──▶ 7. Format response
│                │     {
│                │       totalRegistrations: 2547,
│                │       revenue: 1850000,
│                │       checkInsToday: 1234,
│                │       passDistribution: {...}
│                │     }
└────┬───────────┘
     │
     │ 8. Cache in Redis (5 min TTL)
     ▼
┌──────────┐
│  Redis   │──▶ SET dashboard:stats {...}
└──────────┘
     │
     │ 9. Return JSON
     ▼
┌────────────┐
│ Admin User │──▶ Dashboard updates
└────────────┘    Charts render
                  Stats display
```

---

## 🔐 Security Layers

```
┌─────────────────────────────────────────────────┐
│  Layer 1: Network Security                     │
│  - HTTPS/TLS encryption                        │
│  - Cloudflare DDoS protection                  │
│  - IP whitelisting for admin endpoints         │
└─────────────────────────────────────────────────┘
                    │
                    ▼
┌─────────────────────────────────────────────────┐
│  Layer 2: Application Security                 │
│  - Helmet.js (security headers)                │
│  - CORS (origin whitelisting)                  │
│  - Rate limiting (100 req/15min)               │
│  - Input sanitization                          │
└─────────────────────────────────────────────────┘
                    │
                    ▼
┌─────────────────────────────────────────────────┐
│  Layer 3: Authentication                       │
│  - JWT tokens (access + refresh)               │
│  - Password hashing (bcrypt, 10 rounds)        │
│  - Email verification                          │
│  - Session management (Redis)                  │
└─────────────────────────────────────────────────┘
                    │
                    ▼
┌─────────────────────────────────────────────────┐
│  Layer 4: Authorization                        │
│  - Role-based access control (RBAC)            │
│  - Permission checks per endpoint              │
│  - Admin role hierarchy                        │
└─────────────────────────────────────────────────┘
                    │
                    ▼
┌─────────────────────────────────────────────────┐
│  Layer 5: Data Protection                      │
│  - Parameterized queries (Prisma)              │
│  - Input validation (Zod schemas)              │
│  - XSS prevention                              │
│  - SQL injection prevention                    │
└─────────────────────────────────────────────────┘
                    │
                    ▼
┌─────────────────────────────────────────────────┐
│  Layer 6: Payment Security                     │
│  - Razorpay signature verification             │
│  - Webhook authentication                      │
│  - Secure token storage                        │
│  - PCI DSS compliance (via Razorpay)           │
└─────────────────────────────────────────────────┘
                    │
                    ▼
┌─────────────────────────────────────────────────┐
│  Layer 7: Monitoring & Auditing                │
│  - Audit logs for admin actions                │
│  - Error tracking (Sentry)                     │
│  - Access logs                                 │
│  - Suspicious activity alerts                  │
└─────────────────────────────────────────────────┘
```

---

## 📊 Database Relationships Diagram

```
┌─────────────┐           ┌──────────────┐
│    users    │◀─────────▶│    passes    │
│             │  1    ∞   │              │
│ - id        │           │ - id         │
│ - email     │           │ - user_id    │
│ - password  │           │ - pass_id    │
└──────┬──────┘           │ - pass_type  │
       │                  └──────┬───────┘
       │                         │
       │  1                      │  1
       │                         │
       │  ∞                      │  1
       │                         │
┌──────▼──────────┐       ┌──────▼────────────┐
│ event_          │       │  transactions     │
│ registrations   │       │                   │
│                 │       │ - id              │
│ - id            │       │ - pass_id         │
│ - user_id       │       │ - razorpay_*      │
│ - event_id      │       │ - amount          │
└──────┬──────────┘       └───────────────────┘
       │
       │  ∞
       │
       │  1
       │
┌──────▼──────┐           ┌──────────────┐
│   events    │           │  check_ins   │
│             │           │              │
│ - id        │           │ - id         │
│ - title     │◀──────────│ - pass_id    │
│ - category  │  1    ∞   │ - event_id   │
│ - date      │           │ - scanned_by │
└─────────────┘           └──────┬───────┘
                                 │
                                 │  ∞
                                 │
                                 │  1
                                 │
                          ┌──────▼───────┐
                          │ admin_users  │
                          │              │
                          │ - id         │
                          │ - email      │
                          │ - role       │
                          └──────┬───────┘
                                 │
                                 │  1
                                 │
                                 │  ∞
                                 │
                          ┌──────▼───────┐
                          │ audit_logs   │
                          │              │
                          │ - id         │
                          │ - admin_id   │
                          │ - action     │
                          └──────────────┘
```

---

## 🎯 Performance Optimization Strategy

```
┌────────────────────────────────────────────┐
│  Frontend Optimization                     │
│  - Code splitting (React.lazy)             │
│  - Image optimization (WebP)               │
│  - CDN for static assets                   │
│  - Gzip compression                        │
└────────────────────────────────────────────┘
                    │
                    ▼
┌────────────────────────────────────────────┐
│  API Layer Optimization                    │
│  - Response caching (Redis)                │
│  - Query result caching                    │
│  - Connection pooling                      │
│  - Async/await patterns                    │
└────────────────────────────────────────────┘
                    │
                    ▼
┌────────────────────────────────────────────┐
│  Database Optimization                     │
│  - Indexes on foreign keys                 │
│  - Indexes on search columns               │
│  - Query optimization                      │
│  - Read replicas (if needed)               │
└────────────────────────────────────────────┘
                    │
                    ▼
┌────────────────────────────────────────────┐
│  Caching Strategy                          │
│  - Session data (Redis)                    │
│  - API responses (5-15 min TTL)            │
│  - Static content (CDN)                    │
│  - Database query results                  │
└────────────────────────────────────────────┘
```

---

**This diagram provides a complete visual overview of the E-Summit 2026 backend architecture!**

For implementation details, refer to `BACKEND_ARCHITECTURE.md`
