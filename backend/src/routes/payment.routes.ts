// Deprecated: payment routes have been removed in favor of KonfHub + simplified webhook/pass flows.
// This file remains as a stub to avoid accidental imports elsewhere.
import { Router } from 'express';

const router = Router();

// No routes exported — deprecated.
export default router;
